
def mi_funcion_espectacular # Convenciónpara nombrar las cosas

no_de_var = 2 # variables

NAME = "Carlos"

def __cualquier_cosa() # Método privado

def _cualquier_cosa() # Método protegido

class MiClase: # Clase


if a is not b != if not a is b # suempre utilizar evaliaciones negativas

len if(len(lista)==0) == if not lista # preferir validaciones de 0 sencillas

#? siempre los imports en las primeras lineas

#* excepción
# 
#-*coding: iso-8859-1 -*- #* la codificación del archivo es lo unico que puede ir antes de los imports 

import mi_funcion from el_modulo

import el_modulo #!nunca se debe de importar todo el modulo así

import . from el_modulo